plugin: aws_ec2
regions:
  - us-east-1  # Replace with your AWS region
keyed_groups:
  - key: tags.Name
    prefix: tag_Name_
filters:
  tag:Name: redis_servers  # Filter for instances with the Name tag "redis_servers"
  instance-state-name: running